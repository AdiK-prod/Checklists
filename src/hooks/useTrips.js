// Stub for Module 1 — implemented in Module 7 with Supabase query.
// Fetches trips + trip_travellers + checklist progress for a household.
export function useTrips(_householdId) {
  return { trips: [], loading: false, error: null }
}
