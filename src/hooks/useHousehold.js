// Stub for Module 1 — implemented in Module 7 with Supabase query.
// Fetches household record + household_members for the authenticated user.
export function useHousehold(_householdId) {
  return { household: null, members: [], loading: false, error: null }
}
